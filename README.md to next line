#include <stdio.h>
int main() {
int temp;
printf("Enter the temperature in Celsius: ");
 scanf("%d", &temp);
 if (temp >= 30) {
 printf("It's hot\n");
    } 
else {
 printf("It's cold\n");
    }
    return 0;
}
